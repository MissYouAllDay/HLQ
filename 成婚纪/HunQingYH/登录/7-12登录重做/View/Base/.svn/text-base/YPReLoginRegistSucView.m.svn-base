//
//  YPReLoginRegistSucView.m
//  HunQingYH
//
//  Created by Else丶 on 2018/11/6.
//  Copyright © 2018 YanpengLee. All rights reserved.
//

#import "YPReLoginRegistSucView.h"

@implementation YPReLoginRegistSucView

+ (instancetype)yp_ReLoginRegistSucView{
    YPReLoginRegistSucView *view;
    if (!view) {
        view = [[[NSBundle mainBundle]loadNibNamed:@"YPReLoginRegistSucView" owner:nil options:nil]lastObject];
    }
    return view;
}

- (void)setPassBtn:(UIButton *)passBtn{
    _passBtn = passBtn;
    _passBtn.layer.cornerRadius = 3;
    _passBtn.clipsToBounds = YES;
}

- (void)setPersonInfoBtn:(UIButton *)personInfoBtn{
    _personInfoBtn = personInfoBtn;
    _personInfoBtn.layer.cornerRadius = 3;
    _personInfoBtn.clipsToBounds = YES;
}

/*
// Only override drawRect: if you perform custom drawing.
// An empty implementation adversely affects performance during animation.
- (void)drawRect:(CGRect)rect {
    // Drawing code
}
*/

@end
